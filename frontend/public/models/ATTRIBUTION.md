# Anatomical body model

`athlora-anatomy.glb` is the `base.glb` model supplied with the Athlora Fitness implementation references. It is used only for the static, anatomical Fitness body viewer and contains no animations or character accessories.
